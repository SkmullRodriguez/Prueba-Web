@model IEnumerable<Prueba_Web.Models.tbl_Permisos>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Permisos</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha_permiso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th>
                <label>Usuario</label>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_permiso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item._Usuarios.Nombre)
                @Html.DisplayFor(modelItem => item._Usuarios.Apellido)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_permiso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_permiso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_permiso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
